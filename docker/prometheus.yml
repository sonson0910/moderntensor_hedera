# Prometheus configuration for ModernTensor
# Used by docker-compose monitoring profile

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "moderntensor-validator"
    static_configs:
      - targets: ["validator:9090"]
        labels:
          instance: "validator-1"
